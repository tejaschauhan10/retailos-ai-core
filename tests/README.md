# Tests

Run: `bunx vitest run`

Layout:

* `unit/` — pure logic & small components
* `integration/` — feature flows spanning multiple modules
* `fixtures/` — deterministic sample data
* `mocks/` — hand-written mocks for external services
